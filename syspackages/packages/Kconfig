

config OS_PERL
	bool "Perl"
	default y

config OS_PYTHON
	bool "Python"
	default y

config OS_ZLIB
	bool "Zlib zlib.h"
	depends on OS_PERL
	default y

config OS_LIBMD
	bool "md5.h"
	default y

config OS_OPENSSL
	bool "OpenSSL"
	depends on OS_PERL
	depends on OS_ZLIB	
	default y
