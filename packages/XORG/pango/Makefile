wget https://download.gnome.org/sources/pango/1.50/pango-1.50.12.tar.xz


mkdir build && cd build
meson --prefix=/usr --buildtype=release --wrap-mode=nofallback ..

Dependency 'fribidi' fontconfig cairo
